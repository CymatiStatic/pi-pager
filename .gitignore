# User-specific runtime data (generated by install.ps1, contains private ntfy topic)
scripts/notify.config.json

# Runtime logs + state (should live in ~/.pi-notify/ anyway, but guard here too)
*.log
*.pid
inbox.log
daemon.log
daemon.pid

# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*~

# PowerShell module cache
**/PSScriptAnalyzerResults.xml
